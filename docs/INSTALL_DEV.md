# 개발 환경 안내

1. Python 3.11 이상을 준비합니다.
2. `pip install -r requirements.txt`로 의존성을 설치합니다.
3. `python main.py`로 앱을 실행합니다.
4. `python -m unittest discover -s tests`로 기본 테스트를 돌립니다.

API 키가 없으면 앱은 모의 데이터 모드로 실행됩니다.
